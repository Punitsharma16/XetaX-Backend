package com.xetax.crm.whatsapp.enums;

/** TEXT and TEMPLATE are implemented; the rest are reserved for media/interactive support. */
public enum WhatsAppMessageType {
    TEXT, TEMPLATE, IMAGE, DOCUMENT, VIDEO, AUDIO, INTERACTIVE, REACTION
}
